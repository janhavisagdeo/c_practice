RETURN FUNCTION 

#include <stdio.h>

int main(int argc, const char * argv[]) {

    int first, second, third;

    printf("Please enter the values for first, second and three by letting a space between them:");
    scanf("%d %d %d", &first, &second, &third);

    printf("The values you have entered are\nfirst : %d\nsecond : %d\nthird : %d\n", first, second, third);

    return (111);
}
