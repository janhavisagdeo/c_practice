ARITHMETIC OPERATIONS

#include <stdio.h>

int main(int argc, const char * argv[]) {

    int z, m = 3, n = 2;
    double x, y, p = 2.0;

    x = m / p;
    y = m / n;
    z = m / p;

    printf("X : %f\nY: %f\nZ: %d\n\n", x, y, z);

    return 0;
}
